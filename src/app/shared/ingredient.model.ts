export class Ingredient {
  // Unecessary if public on constructor arguments
  //   public name: string;
  //   public amount: number;

  constructor(public name: string, public amount: number) {
    // Unecessary if public on constructor arguments
    // this.name = name;
    // this.amount = amount;
  }
}
